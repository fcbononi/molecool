#checkpoint
git status
#tell if in git directory or not
ls -la # see ".git" directory
#start version control
git init
#see master heading
git log
# accept changes
git add filename
# change all files
git add .
#commit with message about what the commit is
git commit -m "message"

#go back to last version
git restore
